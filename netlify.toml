# netlify.toml

[build]
  # 构建命令
  command = "pip install -r requirements.txt && mkdocs build"
  # 发布目录 (MkDocs 默认生成在 site 目录)
  publish = "site"

[build.environment]
  # 指定 Python 版本，建议与你本地一致，比如 3.8 或 3.9
  PYTHON_VERSION = "3.11"